# 恶意域名家族之全员通缉（获取已知恶意域名家族的未知域名）

## 设计说明

本题中，选手对给出域名的流量数据进行分析，按照请求特征对域名进行分类。

有标签的域名只占所有恶意域名的50%，请选手找出另外那50%的恶意域名并正确分类。

注意该恶意域名不是 DGA 域名。

## 数据说明

时间范围：2020-03-01 - 2020-05-31 (无 2020-4-28 数据)

域名数量: 总量约20000个，其中恶意域名约1000个

* fqdn.csv: 所有待分类域名和flint rdata中出现的域名
  * encoded_fqdn 域名代号，保留字符串特征
    格式：编码后域名.tld，a表示字母、0表示数字、[aaa]表示aaa为一个词语、特殊符号无变化，如：
    abchello-12.com -> aaa[aaaaa]-00.com
    access2u.org -> [aaaaaa]0a.org
  * fqdn_no 域名编号

* ip.csv / ipv6.csv: 所有客户端 IP 和 Flint 解析结果中出现的 IP 的信息
  * encoded_ip 加密后的IP
  * country,subdivision,city,latitude,longitude IP地理信息
  * isp ISP信息

* access.csv: 客户端访问域名记录, 按10分钟聚合
  * fqdn_no 域名编号
  * encoded_ip 加密后的IP
  * count 请求量
  * time 请求时间

* flint.csv: 域名解析记录, 包括被解析到的域名(cname ...)的 A 和 AAAA 记录
  * fqdn_no_x 域名编号
  * flintType 解析类型
  * encoded_value 解析结果（域名编号或加密后IP）
  * requestCnt 访问量
  * date 日期

* label.csv: 训练数据标签
  * fqdn_no 域名编号
  * family_no 恶意域名家族编号
* whois.json: 相关域名whois信息

## 持续时间

本题的答题时间为2020年8月1日至2020年8月13日

## 提交形式和文件格式

选手提交分类结果 label.csv 及相关 writeup，统一使用zip格式打包提交。注意，压缩文件请勿加密，并确保可以使用 unzip 命令无参数解压。请勿压缩文件夹。

提交文件的命名规则和内容如下：

* label.csv

  对恶意域名的多分类结果，格式与示例 label.csv 同。

  注意结果中只需包含恶意域名。在判分阶段最多只取前2000个结果。


## 提交规则

选手在本题的持续时间内，提交次数不限。

主办方将不定期进行阶段评分，将队伍本题的得分以及当前得分排名范围反馈给选手。在答题截止前，选手可以根据阶段反馈情况调整答案。

本题最终得分为选手在2020年8月13日23时59分59秒答题截止前提交**最后一次提交结果**的得分。

## 评分规则

该题按前2000条预测结果的`Macro F1值 * 100`作为最后得分，满分100分。

该题占总分的权重为 0.2

## 其他

主办方拥有对题目内容和评分规则的解释权，并保留在极端情形下更正题目内容和评分规则的权利。
