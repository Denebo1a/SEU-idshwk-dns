# CLAUDE.md 网络入侵检测与数字取证大作业实验

## 实验概述
大作业实验分为三个任务，题目说明与提交说明位于 `dns-homework/docs/`。
参考资料位于 `dns-homework-reference/`，包含学长的实验源码、文档与结果，可用于对照思路，但应以本组目录和当前题目要求为准。

## 三道实验题目概览

### DNS1：恶意域名家族发现与分类
- 题目：`恶意域名家族之全员通缉（获取已知恶意域名家族的未知域名）`
- 目标：已知仅有约 50% 的恶意域名带标签，需要从全部域名中找出剩余恶意域名，并按恶意家族编号 `family_no` 分类。
- 说明：题目明确指出该恶意域名**不是 DGA 域名**。
- 数据时间范围：`2020-03-01` 至 `2020-05-31`，缺少 `2020-04-28`。
- 数据规模：总域名约 `20000` 个，其中恶意域名约 `1000` 个。
- 主要数据：
  - `fqdn.csv`：域名编号 `fqdn_no` 与保留字符串特征的 `encoded_fqdn`
  - `ip.csv` / `ipv6.csv`：客户端 IP / 解析结果 IP 及地理信息、ISP 信息
  - `access.csv`：按 **10 分钟** 聚合的访问记录，字段含 `fqdn_no`、`encoded_ip`、`count`、`time`
  - `flint.csv`：解析记录，含 `flintType`、`encoded_value`、`requestCnt`、`date`
  - `label.csv`：训练标签，字段为 `fqdn_no,family_no`
  - `whois.json`：相关域名 whois 信息
- 评分：前 `2000` 条预测结果的 `Macro F1 * 100`
- 权重：`0.2`

### DNS2：黑白域名二分类
- 题目：`猜猜我是谁之黑白域名现形记（域名分类）`
- 目标：基于流量与解析特征，将域名进行黑/白二分类。
- 数据时间范围：`2020-03-01` 至 `2020-05-31`，缺少 `2020-04-28`。
- 数据组成：
  - `5_question_train.zip`：带标签训练集
  - `5_question_test.zip`：不带标签测试集，格式与训练集相同
- 主要字段：
  - `fqdn.csv`：`fqdn_no`、`encoded_fqdn`
  - `ip.csv` / `ipv6.csv`：IP 地理信息与 ISP
  - `access.csv`：按 **小时** 聚合，含 `fqdn_no`、`ip_cnt`、`request_cnt`、`total_request`、`date`、`hour`
  - `flint.csv`：解析记录，含 `fqdn_no`、`flintType`、`encoded_value`、`requestCnt`、`date`
  - `label.csv`：训练标签，字段为 `fqdn_no,label`
- 标签说明：提交时沿用训练数据中的 `0/1` 标签体系。
- 评分：`F1 * 100`
- 权重：`0.2`

### DNS3：域名聚类
- 题目：`猜猜我是谁之域名大分类（域名聚类）`
- 目标：对给定域名按请求特征进行聚类，**簇数不能小于 3**。
- 数据时间范围：`2020-03-01` 至 `2020-05-31`，缺少 `2020-04-28`。
- 主要数据：
  - `fqdn.csv`：`fqdn_no`、`encoded_fqdn`
  - `ip.csv` / `ipv6.csv`：IP 地理信息与 ISP
  - `access.csv`：客户端访问域名记录，按 **小时** 聚合，含 `fqdn_no`、`encoded_ip`、`request_cnt`、`total_request`、`date`、`hour`
  - `flint.csv`：解析记录，含 `flintType`、`encoded_value`、`requestCnt`、`ttl`、`date`
  - `label.csv`：示例提交格式，字段为 `fqdn_no,label`
- 评分：`Normalized Mutual Information (NMI) * 100`
- 权重：`0.3`

## 统一提交要求
- 每组提交一个 Gitee 仓库，仓库名固定为 `dns-homework`。
- 评分程序**只读取** `answers/` 目录下的三个结果文件：
  - `answers/dns1.csv`
  - `answers/dns2.csv`
  - `answers/dns3.csv`
- 推荐仓库结构：
  - `README.md`
  - `src/`：源码
  - `docs/`：实验报告（如 PDF）
  - `answers/`：最终提交 CSV
- 所有 CSV 要求：
  - UTF-8 编码
  - 英文逗号分隔
  - 第一行为表头
  - 每个结果文件仅两列
- 各题结果文件格式：
  - `answers/dns1.csv`：表头 `fqdn_no,family_no`
  - `answers/dns2.csv`：表头 `fqdn_no,label`
  - `answers/dns3.csv`：表头 `fqdn_no,label`
- 注意事项：
  - `fqdn_no` 必须使用题目原始编号，不能改名
  - 第二列只能填写数字编号，不能填写中文、英文类别名或概率
  - DNS1 第二列名必须是 `family_no`；DNS2/DNS3 第二列名必须是 `label`
  - DNS3 的聚类簇编号可为 `0,1,2,3,...`，但簇数不能少于 3
  - 对 DNS1，仅需提交恶意域名结果；判分阶段最多取前 `2000` 条
  - 不认识或未提交的样本不会得分，评分程序会按已命中的测试样本比例折算

## 当前目录概述
- `dns-homework`：我们小组自己的实验工作目录
  - `src/`：实验源码目录
  - `questions/`：实验数据集目录，体积较大且通常不纳入 git；除非明确需要，不主动读取大体量数据文件
  - `docs/`：三个实验题目的描述文档与提交说明
  - `answers/`：实验结果目录
- `dns-homework-reference`：学长的实验源码/文档/结果参考
  - `src/`：参考源码目录
  - `docs/`：参考文档目录
  - `answers/`：参考结果目录

## 本仓库协作提示
- 分析与实现时，优先依据 `dns-homework/docs/` 中的当前题目要求。
- 若参考 `dns-homework-reference/`，应注意区分“可借鉴的思路”和“当前实验的正式提交规范”。
- 在未明确需要时，不要直接扫描或读取 `questions/` 下的大体量原始数据。

## 三道题的可执行实现计划

### DNS1 实现计划
1. **确认数据布局**
   - 确认 `questions/` 下 DNS1 对应数据是否包含 `fqdn.csv`、`ip.csv`、`ipv6.csv`、`access.csv`、`flint.csv`、`label.csv`、`whois.json`。
   - 明确 `fqdn_no` 最大编号，避免直接写死数组长度。
2. **完成统一的数据读取脚本**
   - 将 `ip.csv` 和 `ipv6.csv` 合并为统一 IP 信息映射。
   - 将 `access.csv`、`flint.csv` 按域名编号聚合，减少后续重复扫描。
3. **实现特征工程**
   - `fqdn` 字符串特征：长度、数字数、普通字符数、特殊字符数、词段字母数、域名深度、常见后缀标记。
   - `access` 行为特征：总访问量、重复访问量、访问 IP 数、访问国家/城市/ISP 数、按小时分布、按日期分布。
   - `flint` 特征：解析总量、按日期解析分布。
   - `whois` 特征：创建/过期时间、更新时间数、nameserver/邮箱/国家/注册商等计数。
   - 缺失值统一填充策略要固定，例如 `0` 或 `-1`。
4. **先做“恶意检测”再做“家族分类”**
   - 阶段一：将带标签样本视为恶意样本，从无标签样本中随机抽取一批作为临时非恶意样本，训练二分类模型。
   - 多轮重复训练与预测，对无标签样本做投票/平均，筛出高置信恶意域名。
   - 阶段二：仅使用已知恶意训练集，对筛出的恶意域名做多分类，输出 `family_no`。
5. **验证与调优**
   - 检查筛出的恶意域名数量是否与题面“约 1000 个恶意域名、已有约 500 个标签”大致一致。
   - 对家族预测结果统计类别分布，避免极端偏向单一类别。
6. **生成提交文件**
   - 输出 `answers/dns1.csv`，表头必须为 `fqdn_no,family_no`。
   - 只保留恶意域名结果，并控制总量不超过评分重点关注的前 `2000` 条。

### DNS2 实现计划
1. **读取训练集与测试集**
   - 分别读取 `5_question_train/` 与 `5_question_test/` 下的数据文件。
   - 保证训练与测试使用完全一致的特征提取流程。
2. **构建监督学习特征**
   - `fqdn` 字符串特征。
   - `access` 统计特征：总访问量、连续访问量、访问 IP 数、按小时分布、按日期分布。
   - `flint` 特征：解析总量、按日期分布。
   - IP 相关特征先作为可选项；若缺失较多或效果差，可先不纳入主模型。
3. **训练二分类模型**
   - 以训练集 `label.csv` 为监督标签，训练黑/白域名分类器。
   - 先建立可复现基线，再做参数调优、交叉验证或阈值分析。
4. **评估与误差分析**
   - 在训练集内部划分验证集，观察 F1，而不是只看准确率。
   - 若类别不均衡，优先处理阈值、样本权重或特征质量。
5. **输出结果**
   - 对测试集做预测，输出 `answers/dns2.csv`。
   - 表头必须为 `fqdn_no,label`，标签沿用训练集 `0/1` 体系。

### DNS3 实现计划
1. **读取并聚合数据**
   - 读取 `fqdn.csv`、`ip.csv`、`ipv6.csv`、`access.csv`、`flint.csv`。
   - 统一按域名编号构建聚类用特征表。
2. **构建聚类特征**
   - `fqdn` 字符串特征。
   - `access` 特征：总访问量、重复访问量、访问 IP/国家/城市/ISP 数。
   - `flint` 特征：解析总量、TTL 统计、解析分布。
   - 对高维时间序列特征单独处理：可直接保留，也可压缩为统计量（最大值、最小值、中位数、标准差、峰值位置等）。
3. **特征缩放与降维实验**
   - 先做归一化（如 MinMaxScaler）。
   - 视情况尝试 PCA/不使用 PCA 两条路线，比较聚类质量与结果分布。
4. **聚类建模**
   - 以 `k >= 3` 为前提，优先尝试 KMeans，逐步比较不同簇数。
   - 结合簇大小分布和内部指标（如 CH 分数）判断是否存在明显失衡。
5. **结果检查与提交**
   - 检查是否出现极端簇塌缩。
   - 输出 `answers/dns3.csv`，表头为 `fqdn_no,label`，标签为簇编号。

## 参考实现分析（dns-homework-reference）

### 参考目录内容
- 文档：`dns-homework-reference/docs/入侵检测与数字取证大作业报告.md`
- 源码：`dns-homework-reference/src/label-1.ipynb`、`label-2.ipynb`、`label-3.ipynb`
- 结果：`dns-homework-reference/answers/label-1.txt`、`label-2.txt`、`label-3.txt`

### 参考实现的总体风格
- 以 **Jupyter Notebook** 为主，每题一个 notebook。
- 以 **手工特征工程 + 传统机器学习** 为核心，而不是深度学习。
- 数据读取以 `csv.reader` + 原生 Python 容器为主，大量使用列表、字典、`set()` 做聚合计数。
- 许多特征直接按 `fqdn_no` 数字下标存入大数组，追求实现直接、运行简单。

### DNS1 参考实现是如何做的
- 对全部域名构建统一特征向量，来源包括：
  - `access`：访问总量、重复访问量、访问 IP 数、访问国家/城市/ISP 数、24 小时分布、91 天分布
  - `flint`：解析总量、91 天分布
  - `whois`：创建/过期时间、更新时间数、nameserver/邮箱/国家/注册商等计数
  - `fqdn`：长度、数字数、普通/特殊字符数、词段字母数、层级深度、常见后缀标记
- 第一阶段将**所有有标签样本直接视为恶意样本**，再从无标签样本中随机抽取约 `1600` 条作为临时负样本，训练 XGBoost 二分类器。
- 该二分类过程重复约 `500` 轮，对每个无标签样本的预测做平均/投票，再筛出预测为恶意的域名。
- 第二阶段只用已知恶意训练样本训练 XGBoost 多分类器，对筛出的恶意域名预测 `family_no`。
- 最终输出中会保留原始训练标签，再拼接新发现的恶意域名预测结果。

### DNS2 参考实现是如何做的
- 分别读取训练集和测试集。
- 使用与 DNS1 相近的基础特征，但**没有使用 IP 地理信息特征作为主特征**。
- 主要保留：
  - `fqdn` 字符串特征
  - `access` 总量、重复访问量、访问 IP 数、按小时分布、按日期分布
  - `flint` 解析总量、按日期分布
- 使用 XGBoost 二分类直接训练并预测测试集。
- 输出 `fqdn_no,label` 两列结果。

### DNS3 参考实现是如何做的
- 先构造无监督聚类特征，包含：
  - `fqdn` 字符串特征
  - `access` 总量、重复访问量、访问 IP/国家/城市/ISP 数
  - `flint` 解析总量
  - 时间分布特征有两种尝试：
    - 直接拼接完整的按小时/按日期分布
    - 将时间分布压缩成统计量，如最小值、最大值、对应位置、中位数、标准差
- 先对特征做 `MinMaxScaler` 归一化。
- 使用 `KMeans(n_clusters=5)` 聚类。
- 报告中提到：更平衡的簇分布不一定带来更高得分，说明无监督题需要以实际评分结果为准，而不是只看簇大小是否均衡。

### 参考实现中可以借鉴的地方
- **统一的特征工程框架**：三题大量复用同一套 `fqdn/access/flint/ip/whois` 思路，适合我们抽成公共脚本。
- **把时间分布当成重要信号**：按小时、按日期的访问/解析分布在三题里都被反复使用。
- **利用 `set()` 统计去重特征**：国家数、城市数、ISP 数、nameserver 数这类特征实现简单且有效。
- **DNS1 两阶段建模思路**：先找恶意域名，再做家族分类，和题意高度一致，值得直接借鉴。
- **DNS3 区分“树模型特征”和“距离模型特征”**：对 XGBoost 可保留原始高维时间分布；对 KMeans 应考虑压缩或缩放，以避免少数高维特征主导距离。
- **先做可运行基线再调参**：参考实现虽然参数比较经验化，但流程完整，适合先复现后优化。

### 参考实现的局限与使用时的注意点
- notebook 中存在较多 **写死常量**，如 `tot_fqdn = 20512`、`17468`，迁移时最好改为从数据动态推断。
- 代码以 notebook 为中心，**复用性和模块化较弱**，更适合作为思路参考，不适合原样照搬。
- DNS1 中“无标签即临时非恶意”的假设是为了构造二分类基线，能用，但存在噪声，需要通过多轮抽样来缓解。
- 参数多为经验值，报告中也明确提到属于“玄学调参”，因此不应把参数本身视为最关键资产。
- DNS3 的聚类内部指标与最终得分并不完全一致，说明聚类题要同时关注离线结构和实际提交反馈。
